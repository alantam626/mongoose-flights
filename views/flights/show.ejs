<% if (flight.destinations.length) { %>
<table>
    <thead>
        <tr>
            <th>Date</th>
            <th>Review</th>
            <th>Rating</th>
        </tr>
    </thead>
    <tbody>
        <% <flight.destinations.forEach(function(destination) { %>
            <tr> 
                <td><%= destination.createdAt.toLocalDateString() %></td>
                <td><%= destination.airport %></td>
                <td><%= destination.arrival %></td>
            </tr>
        })
    </tbody>
</table>
}

<%- include('../partials/footer') %>